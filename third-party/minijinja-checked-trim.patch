--- a/src/filters.rs
+++ b/src/filters.rs
@@ -128,6 +128,13 @@
 #[deprecated = "Use the minijinja::functions::Function instead"]
 #[doc(hidden)]
 pub use crate::functions::Function as Filter;
+
+/// Borrowed whitespace boundary shared by ordinary trim and closed text storage.
+pub(crate) fn whitespace_trim_range(value: &str) -> std::ops::Range<usize> {
+    let start = value.len() - value.trim_start().len();
+    let end = start + value[start..].trim_end().len();
+    start..end
+}
 
 /// Marks a value as safe.  This converts it into a string.
 ///
@@ -451,7 +458,7 @@
                 let chars = chars.chars().collect::<Vec<_>>();
                 value.as_str().trim_matches(&chars[..]).to_string()
             }
-            None => value.as_str().trim().to_string(),
+            None => value.as_str()[super::whitespace_trim_range(value.as_str())].to_string(),
         };
         value.preserve_safety(output)
     }
--- a/src/vm/shared.rs
+++ b/src/vm/shared.rs
@@ -1,4 +1,4 @@
-//! Storage-parametric dispatch for the selected non-calling instructions.
+//! Storage-parametric dispatch for the selected instructions and filter calls.
 //!
 //! Ordinary evaluation and closed source-bound evaluation use this same operand
 //! ordering and control flow. Storage decides how a value is represented; it
@@ -17,6 +17,7 @@
     LoadConst(V),
     Add,
     Ne,
+    ApplyFilter(&'source str, Option<u16>, u8),
     Emit,
     PushLoop(u8),
     Iterate(u32),
@@ -41,6 +42,12 @@
     fn add(&mut self, left: Self::Value, right: Self::Value) -> Result<Self::Value, Self::Error>;
     fn ne(&mut self, left: Self::Value, right: Self::Value) -> Result<Self::Value, Self::Error>;
     fn truth(&self, value: &Self::Value) -> Result<bool, Self::Error>;
+    fn apply_filter(
+        &mut self,
+        name: &'source str,
+        arguments: Option<u16>,
+        local: u8,
+    ) -> Result<(), Self::Error>;
     fn emit(&mut self, value: Self::Value) -> Result<(), Self::Error>;
     fn push_loop(&mut self, value: Self::Value, flags: u8, pc: u32) -> Result<(), Self::Error>;
     fn next(&mut self) -> Result<Option<Self::Value>, Self::Error>;
@@ -90,6 +97,7 @@
             let value = storage.ne(left, right)?;
             storage.push(value)?;
         }
+        Op::ApplyFilter(name, arguments, local) => storage.apply_filter(name, arguments, local)?,
         Op::Emit => {
             let value = storage.pop()?;
             storage.emit(value)?;
@@ -133,6 +141,9 @@
         Instruction::LoadConst(value) => Op::LoadConst(value.clone()),
         Instruction::Add => Op::Add,
         Instruction::Ne => Op::Ne,
+        Instruction::ApplyFilter(name, arguments, local) => {
+            Op::ApplyFilter(name, *arguments, *local)
+        }
         Instruction::Emit => Op::Emit,
         Instruction::PushLoop(flags) => Op::PushLoop(*flags),
         Instruction::Iterate(target) => Op::Iterate(*target),
--- a/src/vm/shared/ordinary.rs
+++ b/src/vm/shared/ordinary.rs
@@ -10,6 +10,7 @@
     pub vm: &'borrow Vm<'env>,
     pub state: &'borrow mut State<'template, 'env>,
     pub stack: &'borrow mut Stack,
+    pub loaded_filters: &'borrow mut [Option<&'env Value>],
     pub output: &'borrow mut Output<'output>,
     pub next_loop_recursion_jump: &'borrow mut Option<(u32, bool)>,
     pub undefined_behavior: UndefinedBehavior,
@@ -67,6 +68,29 @@
     fn truth(&self, value: &Value) -> Result<bool, Error> {
         self.undefined_behavior.is_true(value)
     }
+    fn apply_filter(
+        &mut self,
+        name: &'env str,
+        arguments: Option<u16>,
+        local: u8,
+    ) -> Result<(), Error> {
+        let name = crate::vm::normalize_filter_test_name(name);
+        let filter = crate::vm::get_or_lookup_local(self.loaded_filters, local, || {
+            self.state.env().get_filter(name.as_ref())
+        })
+        .ok_or_else(|| {
+            Error::new(
+                ErrorKind::UnknownFilter,
+                format!("filter {} is unknown", name.as_ref()),
+            )
+        })?;
+        let arguments = self.stack.get_call_args(arguments);
+        let count = arguments.len();
+        let value = filter.call(self.state, arguments)?;
+        self.stack.drop_top(count);
+        self.stack.push(value);
+        Ok(())
+    }
     fn emit(&mut self, value: Value) -> Result<(), Error> {
         if self.vm.env.is_default_formatter() {
             if self.strict_undefined
--- a/src/vm/mod.rs
+++ b/src/vm/mod.rs
@@ -329,6 +329,7 @@
                         vm: self,
                         state,
                         stack: &mut stack,
+                        loaded_filters: &mut loaded_filters,
                         output: out,
                         next_loop_recursion_jump: &mut next_loop_recursion_jump,
                         undefined_behavior,
@@ -348,6 +349,7 @@
                 | Instruction::Lookup(_)
                 | Instruction::GetAttr(_)
                 | Instruction::GetItem
+                | Instruction::ApplyFilter(..)
                 | Instruction::LoadConst(_)
                 | Instruction::Add
                 | Instruction::Ne
@@ -561,24 +563,6 @@
                 Instruction::EndCapture => {
                     stack.push(out.end_capture(state.auto_escape.get()));
                 }
-                Instruction::ApplyFilter(name, arg_count, local_id) => {
-                    let normalized_name = normalize_filter_test_name(name);
-                    let filter =
-                        ctx_ok!(get_or_lookup_local(&mut loaded_filters, *local_id, || {
-                            state.env().get_filter(normalized_name.as_ref())
-                        })
-                        .ok_or_else(|| {
-                            Error::new(
-                                ErrorKind::UnknownFilter,
-                                format!("filter {} is unknown", normalized_name.as_ref()),
-                            )
-                        }));
-                    let args = stack.get_call_args(*arg_count);
-                    let arg_count = args.len();
-                    a = ctx_ok!(filter.call(state, args));
-                    stack.drop_top(arg_count);
-                    stack.push(a);
-                }
                 Instruction::PerformTest(name, arg_count, local_id) => {
                     let normalized_name = normalize_filter_test_name(name);
                     let test = ctx_ok!(get_or_lookup_local(&mut loaded_tests, *local_id, || {
--- a/src/bounded/source.rs
+++ b/src/bounded/source.rs
@@ -141,6 +141,7 @@
     Zero,
     Add,
     Ne,
+    Trim,
     Emit,
     PushLoop(u8),
     Iterate(u32),
@@ -451,7 +452,9 @@
                 | Instruction::Emit
                 | Instruction::PushLoop(_)
                 | Instruction::JumpIfFalse(_) => (1, -1),
-                Instruction::GetAttr(_) | Instruction::JumpIfFalseOrPop(_) => (1, 0),
+                Instruction::GetAttr(_) | Instruction::Trim | Instruction::JumpIfFalseOrPop(_) => {
+                    (1, 0)
+                }
                 Instruction::GetItem | Instruction::Add | Instruction::Ne => (2, -1),
                 _ => (0, 0),
             };
--- a/src/bounded/worker.rs
+++ b/src/bounded/worker.rs
@@ -9,6 +9,8 @@
     Source(Range),
     Role(usize),
     Content(usize),
+    RoleSlice(usize, Range),
+    ContentSlice(usize, Range),
 }
 #[derive(Clone, Copy, Debug)]
 pub(super) enum Text {
@@ -17,6 +19,9 @@
         start: usize,
         count: usize,
         bytes: usize,
+        skip: usize,
+        leading: usize,
+        trailing: usize,
     },
 }
 #[derive(Clone, Copy, Debug)]
@@ -78,13 +83,72 @@
         Atom::Source(range) => range.text(&source.bytes).ok_or(Error::Geometry),
         Atom::Role(index) => Ok(messages.get(index).ok_or(Error::Geometry)?.role),
         Atom::Content(index) => Ok(messages.get(index).ok_or(Error::Geometry)?.content),
-    }
+        Atom::RoleSlice(index, range) => range
+            .text(messages.get(index).ok_or(Error::Geometry)?.role.as_bytes())
+            .ok_or(Error::Geometry),
+        Atom::ContentSlice(index, range) => range
+            .text(
+                messages
+                    .get(index)
+                    .ok_or(Error::Geometry)?
+                    .content
+                    .as_bytes(),
+            )
+            .ok_or(Error::Geometry),
+    }
+}
+fn slice_atom(atom: Atom, start: usize, bytes: usize) -> Result<Atom, Error> {
+    let range = |offset: usize| -> Result<Range, Error> {
+        Ok(Range::new(
+            offset.checked_add(start).ok_or(Error::Overflow)?,
+            bytes,
+        ))
+    };
+    Ok(match atom {
+        Atom::Source(original) => Atom::Source(range(original.start)?),
+        Atom::Role(index) => Atom::RoleSlice(index, range(0)?),
+        Atom::Content(index) => Atom::ContentSlice(index, range(0)?),
+        Atom::RoleSlice(index, original) => Atom::RoleSlice(index, range(original.start)?),
+        Atom::ContentSlice(index, original) => Atom::ContentSlice(index, range(original.start)?),
+    })
+}
+// Clip one borrowed atom against a concatenation's retained byte window. Keep
+// empty atoms too, so the exact same finite slot count is known before H.
+fn clip_atom(
+    source: &PreparedTemplate,
+    messages: Messages<'_>,
+    atom: Atom,
+    skip: &mut usize,
+    remaining: &mut usize,
+) -> Result<Atom, Error> {
+    let text = atom_text(source, messages, atom)?;
+    let start = (*skip).min(text.len());
+    *skip -= start;
+    let bytes = (text.len() - start).min(*remaining);
+    text.get(start..start + bytes).ok_or(Error::Geometry)?;
+    *remaining -= bytes;
+    slice_atom(atom, start, bytes)
 }
 impl Engine<'_, '_, '_> {
     fn parts(&self, text: Text) -> Result<(usize, usize), Error> {
         match text {
             Text::Atom(atom) => Ok((1, atom_text(self.source, self.messages, atom)?.len())),
             Text::Concat { count, bytes, .. } => Ok((count, bytes)),
+        }
+    }
+    fn edges(&self, text: Text) -> Result<(usize, usize, usize), Error> {
+        match text {
+            Text::Atom(atom) => {
+                let text = atom_text(self.source, self.messages, atom)?;
+                let kept = crate::filters::whitespace_trim_range(text);
+                Ok((text.len(), kept.start, text.len() - kept.end))
+            }
+            Text::Concat {
+                bytes,
+                leading,
+                trailing,
+                ..
+            } => Ok((bytes, leading, trailing)),
         }
     }
     fn append(&mut self, text: Text) -> Result<(), Error> {
@@ -100,14 +164,31 @@
             }
             match text {
                 Text::Atom(atom) => storage[self.counts.concat] = atom,
-                Text::Concat { start, count, .. } => {
+                Text::Concat {
+                    start,
+                    count,
+                    bytes,
+                    mut skip,
+                    ..
+                } => {
                     let previous_end = start.checked_add(count).ok_or(Error::Overflow)?;
-                    // Existing concat prefixes precede the new append; no
-                    // overwritten slot can still be read by the shared Add.
                     if previous_end > self.counts.concat {
                         return Err(Error::Geometry);
                     }
-                    storage.copy_within(start..previous_end, self.counts.concat);
+                    let mut remaining = bytes;
+                    for offset in 0..count {
+                        let atom = clip_atom(
+                            self.source,
+                            self.messages,
+                            storage[start + offset],
+                            &mut skip,
+                            &mut remaining,
+                        )?;
+                        storage[self.counts.concat + offset] = atom;
+                    }
+                    if skip != 0 || remaining != 0 {
+                        return Err(Error::Geometry);
+                    }
                 }
             }
         }
@@ -236,6 +317,20 @@
         let (rn, rb) = self.parts(right)?;
         let count = ln.checked_add(rn).ok_or(Error::Overflow)?;
         let bytes = lb.checked_add(rb).ok_or(Error::Overflow)?;
+        let (_, ll, lt) = self.edges(left)?;
+        let (_, rl, rt) = self.edges(right)?;
+        let leading = if ll == lb {
+            lb.checked_add(rl).ok_or(Error::Overflow)?
+        } else {
+            ll
+        };
+        let trailing = if leading == bytes {
+            0
+        } else if rl == rb {
+            rb.checked_add(lt).ok_or(Error::Overflow)?
+        } else {
+            rt
+        };
         let start = self.counts.concat;
         self.append(left)?;
         self.append(right)?;
@@ -243,6 +338,9 @@
             start,
             count,
             bytes,
+            skip: 0,
+            leading,
+            trailing,
         }))
     }
     fn ne(&mut self, left: Slot, right: Slot) -> Result<Slot, Error> {
@@ -263,6 +361,38 @@
             Slot::Message(_) | Slot::Loop => Ok(true),
         }
     }
+    fn apply_filter(
+        &mut self,
+        name: &str,
+        arguments: Option<u16>,
+        _local: u8,
+    ) -> Result<(), Error> {
+        if name != "trim" || arguments != Some(1) {
+            return Err(Error::Geometry);
+        }
+        let Slot::Text(text) = self.pop()? else {
+            return Err(Error::Geometry);
+        };
+        let (bytes, leading, trailing) = self.edges(text)?;
+        let bytes = bytes
+            .checked_sub(leading)
+            .and_then(|n| n.checked_sub(trailing))
+            .ok_or(Error::Geometry)?;
+        let trimmed = match text {
+            Text::Atom(atom) => Text::Atom(slice_atom(atom, leading, bytes)?),
+            Text::Concat {
+                start, count, skip, ..
+            } => Text::Concat {
+                start,
+                count,
+                bytes,
+                skip: skip.checked_add(leading).ok_or(Error::Overflow)?,
+                leading: 0,
+                trailing: 0,
+            },
+        };
+        self.push(Slot::Text(trimmed))
+    }
     fn emit(&mut self, value: Slot) -> Result<(), Error> {
         let Slot::Text(text) = value else {
             return Err(Error::Geometry);
@@ -273,6 +403,8 @@
                 start,
                 count,
                 bytes,
+                mut skip,
+                ..
             } => {
                 if self.workspace.output.is_none() {
                     self.counts.output = self
@@ -286,6 +418,7 @@
                 if end > self.counts.concat {
                     return Err(Error::Geometry);
                 }
+                let mut remaining = bytes;
                 for index in start..end {
                     let atom = *self
                         .workspace
@@ -293,7 +426,12 @@
                         .as_deref()
                         .and_then(|v| v.get(index))
                         .ok_or(Error::Geometry)?;
+                    let atom =
+                        clip_atom(self.source, self.messages, atom, &mut skip, &mut remaining)?;
                     self.emit_atom(atom)?;
+                }
+                if skip != 0 || remaining != 0 {
+                    return Err(Error::Geometry);
                 }
                 Ok(())
             }
@@ -388,6 +526,7 @@
                 Instruction::Zero => Op::LoadConst(Slot::Zero),
                 Instruction::Add => Op::Add,
                 Instruction::Ne => Op::Ne,
+                Instruction::Trim => Op::ApplyFilter("trim", Some(1), 0),
                 Instruction::Emit => Op::Emit,
                 Instruction::PushLoop(flags) => Op::PushLoop(flags),
                 Instruction::Iterate(target) => Op::Iterate(target),
@@ -423,7 +562,19 @@
         .checked_add(size_of::<Result<Counts, Failure>>())?
         .checked_add(size_of::<[Slot; 3]>())?
         .checked_add(size_of::<[Frame; 1]>())?
-        .checked_add(size_of::<[Slot; 1]>())
+        .checked_add(size_of::<[Slot; 1]>())?
+        // add: both text operands and the thirteen part/edge/result scalars.
+        .checked_add(size_of::<(Text, Text)>())?
+        .checked_add(size_of::<[usize; 13]>())?
+        // append/emit: input text, retained interval, cursor, and atom transport.
+        .checked_add(size_of::<Text>())?
+        .checked_add(size_of::<[usize; 8]>())?
+        .checked_add(size_of::<(Atom, Result<Atom, Error>)>())?
+        // trim/clip/edge helpers: borrowed values, UTF-8 windows and results.
+        .checked_add(size_of::<(Slot, Text, Text, Atom, Range)>())?
+        .checked_add(size_of::<[usize; 8]>())?
+        .checked_add(size_of::<(&str, &str, std::ops::Range<usize>)>())?
+        .checked_add(size_of::<Result<Range, Error>>())
 }
 
 #[cfg(test)]
@@ -483,6 +634,9 @@
             start: 0,
             count: 1,
             bytes: usize::MAX,
+            skip: 0,
+            leading: 0,
+            trailing: 0,
         });
         assert!(matches!(
             engine.add(huge, Slot::Text(Text::Atom(Atom::Role(0)))),
--- a/src/bounded/template_syntax/emit.rs
+++ b/src/bounded/template_syntax/emit.rs
@@ -154,7 +154,7 @@
             | Instruction::PushLoop(_)
             | Instruction::JumpIfFalse(_)
             | Instruction::JumpIfFalseOrPop(_) => (1, -1),
-            Instruction::GetAttr(_) => (1, 0),
+            Instruction::GetAttr(_) | Instruction::Trim => (1, 0),
             Instruction::GetItem | Instruction::Add | Instruction::Ne => (2, -1),
             _ => (0, 0),
         };
@@ -248,6 +248,10 @@
             RecordKind::Item(value, key) => {
                 self.push(Action::Emit(Instruction::GetItem, span))?;
                 self.push(Action::Expression(*key))?;
+                self.push(Action::Expression(*value))?;
+            }
+            RecordKind::Filter("trim", Some(value), arguments) if arguments.len == 0 => {
+                self.push(Action::Emit(Instruction::Trim, span))?;
                 self.push(Action::Expression(*value))?;
             }
             RecordKind::Binary(BinOpKind::Add, left, right) => {
