--- a/src/models/bpe/compile.rs
+++ b/src/models/bpe/compile.rs
@@ -295,7 +295,8 @@
         data.entries
             .dedup_by(|a, b| bytes[a.start..a.start + a.len] == bytes[b.start..b.start + b.len]);
         data.ids.extend(0..data.entries.len());
-        data.ids.sort_unstable_by_key(|&i| data.entries[i].id);
+        let entries = &data.entries;
+        data.ids.sort_unstable_by_key(|&i| entries[i].id);
         if data
             .ids
             .windows(2)
@@ -559,6 +560,8 @@
         size_of::<Result<BpeCompilePlan<'_>, Error>>(),
         size_of::<BpeCompileRequirements>(),
         size_of::<VocabEntry>(),
+        size_of::<&Vec<VocabEntry>>(),  // split-field sort input
+        size_of::<&&Vec<VocabEntry>>(), // borrowed closure capture
         size_of::<MergeEntry>(),
         size_of::<Partial>(),
         size_of::<Packed>(),
--- a/src/models/bpe/parity_trainer.rs
+++ b/src/models/bpe/parity_trainer.rs
@@ -1639,7 +1639,7 @@
         t_ref.feed_language(1, l1);
         let mut m_ref = BPE::default();
         let (_s, ref_merges) = t_ref.do_train(&mut m_ref).unwrap();
-        let ref_vocab = m_ref.vocab.len();
+        let ref_vocab = m_ref.storage.legacy_vocab().len();
 
         // total_symbols run: target = ref_vocab + N_SPECIALS, with that many
         // special tokens added. Final vocab must equal the target exactly.
@@ -1663,10 +1663,10 @@
         let (_s2, ts_merges) = t_ts.do_train(&mut m_ts).unwrap();
 
         assert_eq!(
-            m_ts.vocab.len(),
+            m_ts.storage.legacy_vocab().len(),
             target,
             "total_symbols=true should make final vocab == target {target}, got {}",
-            m_ts.vocab.len()
+            m_ts.storage.legacy_vocab().len()
         );
         assert_eq!(
             ts_merges.len(),
