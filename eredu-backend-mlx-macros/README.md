# eredu-backend-mlx-macros

Backend-internal derives for MLX module parameter traversal. Direct use is
normally unnecessary; depend on `eredu-backend-mlx` for MLX backend facilities.

Licensed under either Apache-2.0 or MIT.
