# Original distributed capture source construction

The original partition capture declaration now constructs its retained source
under the supplied metadata account, including a fresh artifact identity when
needed. Consuming an already loaded coordinate table does not fund its original
map nodes, names, shapes, ownership rows or support report. Ordinary and funded
construction use the same placement, catalog, support and fingerprint workers.

The sections below are chronological producer and validation checkpoints.
Their pending native runs and intermediate CLI, rotary and collective failures
describe those source snapshots. Later native results include passing YaRN and
InputProducts parity/custody, CPU Host spill/reload and all seven public image/tool
cases, including TP2, PP2 and combined TP2/PP2. Current acceptance and the separate
distributed CLI follow-up are maintained in [native validation](bounded-followup-native.md),
[CLI validation](bounded-followup-cli.md) and [the overview](bounded-followup.md).
The [native work matrix](validation/bounded-followup-native-work-matrix-2026-09-18.json)
records the later passing 25-case mechanism and 15-case custody/reset/copy runs.
Historical commands and failed-run logs remain below; passing one producer does
not establish an unrun application or hardware combination.

## Current routed parameter-source checkpoint

Cold partition destination construction now covers the retained routed and
composite routes as well as dense execution. It uses the selected local or
pipeline dispatch and preserves complete per-unit slot declarations. Exact
exclusions come from static task placement and the whole selected independent
bank catalog, including generated companions; absent local rows do not create
exclusion authority. Composite boundary construction first completes its retained
model graph, while direct equation-source publication keeps its separate policy.

The final neutral architecture suite passes **772 tests, with one ignored**.
Its two new routed/composite Disk destination tests are included in that total;
each checks all 12 ranks across TP2/PP1 and PP2/EP2, repeated projection, exact
physical binding and refusal when a required exclusion is removed. Commands,
source hashes and logs are in
`/private/tmp/native-routed-destinations-validation.json` (SHA-256
`add3f69a1a07ca6b017136806575b003c4126dd7ac1f66163b67adf765426acf`).

The native manager source now pays for one immutable exclusion-name owner and
shares it with native and cold binding. Its original account remains charged until
the final escaping alias retires, and adoption requires the exact selected names. The same admitted
manager retains the complete per-unit constructor inventory, independently of
leased rows, so independently owned bank placeholders remain in the constructor
census. Missing declarations with nonempty exclusions refuse rather than infer
an incomplete inventory. Backend library/test metadata and no-default metadata
pass in **46.58 s** and **36.75 s**, respectively; exact commands and source hashes
are in `/private/tmp/tokenizer-host-exclusion-owner-checks.json` (SHA-256
`c0fb4938dc3e8563da3873471b6abbcf008ecfd39cec01bfa6d06df932899352`).
Those metadata checks did not execute the new owner and constructor fixtures.
The later scoped native backend checkpoint passed **66/66** cases, including
those fixtures. The later selected-executor optimized artifact passes all seven
public cases and all six Dense/routed Resident/Host/Disk CLI cases. Exact records
are linked from [public conformance](bounded-followup-public-conformance.md#qualified-native-public-checkpoint)
and [CLI validation](bounded-followup-cli.md). Both corrected readiness
fixtures also pass native execution, preserving custody through a real pending
child and retiring the original account only after terminal completion.
Cold partitioned prediction-extension destination projection remains unavailable;
this does not refer to ordinary next-token generation, which the base-route
matrix exercises. The cold route returns `PreparedExecutionError::UnavailablePrediction`
and supplies no exact prepared prediction manager. The bounded speculative bank
also rejects distributed execution with `PrefillScopeUnavailable`: its selected
producer supplies one stream and local cancellation. Both restrictions were
already present at the starting commit `77e08045`; they describe an unsupported
bounded mechanism, not an inherent family limitation. Current native and CLI
verdicts remain in the guides linked above; the sections below preserve earlier
checkpoints.

## Shared collection and coordinate storage

`eredu-collections` is a dependency-free `no_std` foundation containing the
existing safe local AVL worker. The default JSON map now imports that worker;
there is no second copy in the JSON fork. Its prospective insertion callback
receives the exact node `Layout`. Existing-key replacement, rotations, removal
and iteration allocate no backing. Admission accounts and output/error custody
remain with the consuming producer. Generic tree tests moved with the worker;
JSON nested serialization and paid Value tests remain in the JSON fork.

The component coordinate inverse now uses this same tree for ordinary and
funded construction and copying. The separate prepared vector inverse and its
preallocated-copy API were removed. Five capture/intervention copy consumers
invoke the shared source clone with their actual existing metadata accounts.
The source's declared index order, duplicate/out-of-range validation, wire
identity and lookup semantics remain unchanged.

Validation:

- Collection behavior, balance and refusal: 4 passed; the comparative measurement
  is separately ignored by default.
- JSON nested serialization plus paid Value parity/refusal: 4 passed in each of
  default sorted and `preserve_order` profiles.
- Core coordinate construction, copies, projection and every reached reservation
  refusal: 6 passed.
- Runtime consumer metadata check: passed. Current behavioral suites pass:
  partition mechanisms 8, intervention projection 2, full partition receipt/
  observer conformance 114, and original fragment sources 12.
- Release measurement against standard `BTreeMap`: 7-entry repeated insert/read/
  iterate/remove workload 1.20 times reference duration; 50,000 shuffled entries
  1.60 times. This measures the unchanged local worker, not a native capture run.

Logs: `/private/tmp/contracts-collections-tests.log`,
`/private/tmp/contracts-collections-json-{sorted,insertion}.log`,
`/private/tmp/contracts-collections-measurement.log`,
`/private/tmp/contracts-coordinate-convergence-tests2.log`, and
`/private/tmp/contracts-coordinate-runtime-check.log`.

## Parameter layout construction

The existing parameter placement algorithm is shared by ordinary construction
and `derive_partitioned_local_layout_with_funding`. The latter returns a closed
`PreparedLocalModelLayout`; errors likewise retain the original account. Names,
shape vectors, segmented physical indices, additional placements, map nodes and
semantic diagnostic text are admitted before their producers. Physical chunk
range validation now returns fixed typed failures without first allocating a
string. The existing equations and ordinary diagnostic text are retained.

Architecture metadata check and all five local-layout tests pass: three new
sharding/refusal/custody tests plus the two existing ordinary equation oracles.
Every reached source reservation is refused independently; failed results retain
the actual account until final retirement. Allocator failures retain their real
source and fixed funding refusals remain reachable through the error chain.
The complete source constructor described below encloses these layout results.

Current logs: `/private/tmp/contracts-coordinate-final.log`,
`/private/tmp/contracts-coordinate-capture-consumers.log`,
`/private/tmp/contracts-coordinate-intervention-consumers.log`,
`/private/tmp/contracts-coordinate-partition-receipts.log`,
`/private/tmp/contracts-coordinate-fragment-sources.log`, and
`/private/tmp/contracts-layout-source-final.log`.

## Ownership and component destinations

Rank ownership now shares one worker across ordinary and source construction.
It pays group/role strings, range vectors and the role-validation tree before
construction. Replica roles retain their existing distinction from invocation
ownership. State slicing remains in the existing ordinary admission stage;
the capture source projection supplies no mutable-state request. Its ranges
already follow cumulative decoder offsets, so the redundant sorting producer
has been removed from both profiles without changing their order.

The four retained component tables now use the same ordered map. Dense component
construction, indexed coordinate projection, grouped read/write projections,
scalar gates and observation registration share prospective destinations.
Funding errors retain their fixed original leaf; semantic diagnostic text is
constructed under the same account before being returned to its enclosing
source. This private producer work does not publish an unowned source.

Validated checkpoint: runtime ownership 2 tests, architecture rank ownership 2,
and component placement 36 tests pass. Every reached destination is refused for
rank, dense component, decoder, stream, transform and prediction construction.
Prediction coverage enters the actual peer parameter-layout compiler; the idle
rank case retains complete declarations while preserving absent ownership.
The full constructor now retains its original account in the closed
`PreparedComponentPartitionSource` and in its typed failure. Ordinary and funded
paths share the selected-rank and optional prediction workers. Logs:
`/private/tmp/contracts-ownership-tests.log`,
`/private/tmp/contracts-rank-source-tests3.log`, and
`/private/tmp/contracts-prediction-source-tests2.log`.

The speculative ancestry resolver supplies borrowed typed facts, so source
callers fund its original diagnostic text without a second semantic resolver.
Canonical source catalog copying now reuses the existing closed point copier,
and ordinary/paid support projection uses one phase/selection/hook worker.
The core field copier preserves every catalog/support field, refuses every
reached destination and retains the original funding leaf. Runtime support
projection passes its nine existing support tests. The complete architecture
source tests pass all three cases: retained-selection refusal/custody, exact
catalog/support parity, and fresh identity construction without ordinary cache
warmup. The freshly compiled component placement suite passes all 36 tests.

Logs: `/private/tmp/contracts-capture-source-copy-tests-final.log`,
`/private/tmp/contracts-capture-support-tests.log`,
`/private/tmp/contracts-fresh-component-source-final-tests.log`, and
`/private/tmp/contracts-fresh-component-placement-tests.log`.

## Fresh artifact identity

Checkpoint fingerprinting owns the original stable-file read/hash worker. Its
prospective policy admits member vectors, role strings, one reused 1 MiB read
buffer, concrete controls, and filesystem diagnostic paths before construction.
Each file is read once; the original before/after metadata and byte-count checks
still reject observable changes. These are version checks, not an atomic
multi-file snapshot. On Unix, a paid terminated path is passed through
the safe `rustix` file-opening API, avoiding an opaque path-conversion allocation.
This is portable host filesystem support, not a native accelerator dependency;
the workspace unsafe-code prohibition is unchanged.

Core applies the same versioned identity transcript to those original member
facts. An iterative ordering worker replaces opaque sort scratch and recursive
control. Ordinary and funded resolution share the same cache and an
allocation-free exclusive resolver claim. A successful digest is fixed-size;
funded refusals and semantic failures leave the source retryable. Escaping
funded failures retain their original account and concrete filesystem cause.
No file is reopened when the exact digest is already cached.
The claim currently uses an atomic flag with yielding waiters rather than a
parking queue. Concurrent digest correctness is covered; contention CPU cost
for large artifacts and fairness are not established by that test.

The checkpoint artifact suite passes nine tests, including Unicode paths,
single-pass reads, changed files, real I/O errors and every reached refusal.
The final review restored the original `File::open` retry behavior for `EINTR`
through Rustix's existing retry worker. Repeated interruptions reuse the same
paid path and controls; terminal errors preserve their original cause. The
focused retry test covers two interruptions followed by success or a terminal
permission error, plus refusal before the syscall and diagnostic destinations.
The final core artifact suite passes 35 tests, including funded semantic
diagnostic refusals, an independent canonical digest transcript and simultaneous
ordinary/funded resolvers. Logs:
`/private/tmp/contracts-fingerprint-review-tests.log` and
`/private/tmp/contracts-fresh-identity-core-final-tests.log`.

## Native source publication

The native adapter combines the exact selected component source with retained
transport descriptors and collector facts. Original capture and intervention
declarations invoke this constructor even when lazy discovery has not run.
Transform filtering and support conditions preserve the existing semantics;
funding refusal cannot become a semantic unsupported point.

The shared source has a closed owner. Its actual shared allocation is paid
before publication and retires before its payload/account; clones share that
owner without new backing. Failed attempts are not cached. A prior ordinary
source is rebuilt through the same funded worker before replacing the cache,
while existing ordinary aliases remain valid. A genuinely funded source can
be reused with its original account; no source adoption or fabricated credit
is exposed. Three private publication/cache tests pass shell refusal and
retirement, every reached cold constructor refusal, ordinary-cache replacement,
surviving old aliases, and reuse of the exact funded owner from either discovery
entry (`/private/tmp/contracts-native-source-owner-review-tests.log`). These
compile the actual private source worker against the built portable core; they
do not execute a native session.

The final entry audit found that a supplied `options.capture` could bypass raw
declaration compilation. Its first cold quotation previously read an absent or
ordinary partition cache before candidate planning created its account. The
shared original text admission now starts its capture metadata account before
qualifying that cache or revalidating capture, for both original token IDs and
completed media inputs. The account uses the loaded pool, exact execution
identity and configured capacity. A cold or ordinary cache invokes the same
source constructor; an already funded publication is reused. The supplied
capture plan remains unchanged; the source is neither adopted nor re-admitted.
The admission and returned quotation retain the actual new metadata account
through their corresponding payloads and rejection paths.

## Remaining validation and platform work

Fresh funded file opening is currently qualified on Unix. Other hosts retain
ordinary identity behavior and may consume an already resolved fixed digest;
their fresh funded opening remains an explicit implementation gap. This is not
an inherent artifact or model limitation. The native adapter's library and test
sources pass Rust metadata checking with `DOCS_RS=1` and the `metal` feature
(`/private/tmp/contracts-native-fresh-source-final-check.log`). This does not execute
native operators, transfers or collectives.

At this source checkpoint the public no-backend conformance run passed 125 backend tests and 34
portable facade tests; two external checkpoint fixtures remain explicitly
ignored. The build took 56.00 seconds; the suites ran in 9.01 and 14.52 seconds.
Log: `/private/tmp/contracts-partition-source-public-conformance.log`.
At that checkpoint, native distributed execution and whole-stack cold-constructor
closure were separate validation obligations. The subsequent public matrix and
cold-producer checks are linked above; these early producer tests alone do not
establish either.

The subsequent cold rejection review closed those adjacent producers as well.
Capture revalidation now uses core's fixed borrowed declaration validator with
the actual selected catalog, capability and phase facts. Comparison controls
are paid before traversal. Selection, host-plan and validation failures use
the existing counted NN error allocation, enclosing the original typed cause
and its actual metadata account. Its final shared allocation retires before
that account. Exact prepared-path validation also receives the same metadata
policy instead of allocating an unpriced native error box. Its default absent
implementation returns a fixed typed native variant. Ordinary callers retain
their explicit ordinary construction policy on the same validation worker.

The two early capture source pins now use the existing counted
`ExistingStoragePinLayout` and a prospectively paid one-entry input vector in
both profiles. This preserves existing-only pin semantics and the exact source
identities; no missing source is registered and no storage credit is created.
The capture admission/quotation encloses the pin metadata account.

Two direct neutral tests pass: ordinary-versus-borrowed decision parity across
catalog/capability/phase changes, and every reached comparison/error reservation
refusal with exact typed source and final-drop account custody. Log:
`/private/tmp/contracts-cold-capture-validation-tests.log`. The combined current
backend library and test metadata check passes in 53.12 seconds, including the
new Ring hook and concurrent native cold-quote diagnostics:
`/private/tmp/contracts-cold-capture-native-check2.log`. Native execution remains
separate from this compile-only result.

The original speculative entry selects its funded driver before the ordinary
partition binding branch; the ordinary binding is not a fallback after a
funded refusal. This source audit does not claim that unrelated cold producers,
other capture transformations or the complete distributed generation lifecycle
have all been validated.

## Small native regression set

The existing two-rank Ring fixtures are the smallest applicable real distributed
checks on one Metal host. They spawn their own local worker processes and use
the inherited `EREDU_TEST_RING_DEVICE=gpu`; no multi-host launcher is required.
Run these exact backend library tests in sequence:

- `tests::distributed_pipeline_ring::ring_public_component_capture_qwen3_tensor`
- `tests::distributed_pipeline_ring::ring_public_component_capture_qwen3_pipeline_streamed`
- `tests::distributed_pipeline_ring::ring_public_component_capture_qwen3_tensor_host`

Their component fixture now also uses its real loaded pool, execution and
transport to check cold source publication and ordinary-cache replacement.
It retains the actual already compiled capture plan, verifies revalidation
against both publications, refuses an exhausted real pool before publication,
checks unchanged ordinary identity on refusal, and checks account retirement
after the final funded alias. Repeated funded lookup must make no new charge.
The fixture then continues its existing ordinary capture and numerical oracle.
The first actual two-rank tensor run passed this source-publication hook and
the component snapshot/replay checks, then failed a later parameter fixture
assertion. That fixture installs replacements directly into declared model
slots, so requiring a positive count in the separate enclosing overlay map was
incorrect. Its corrected assertions retain exact map counts and require one
original/replacement/value oracle per declared slot; native replacement,
coordinated rollback and restoration comparisons remain intact.

The first streamed and host runs stopped during loading: the fixture had
constructed ordinary GPU streams before bounded materialization requested the
native Device producer. Opaque Ring workers now use the existing prepared
distributed backend factory before any ordinary Device/Stream construction,
retaining its actual execution and source owners. No ordinary-predecessor
rejection or source qualification was relaxed. The three complete native runs
now pass after these fixture corrections. Initial diagnostic logs:
`/private/tmp/eredu-native-work-ring-tensor.log`,
`/private/tmp/eredu-native-work-ring-streamed.log`, and
`/private/tmp/eredu-native-work-ring-host.log`.

The combined backend library and test metadata check passes with these fixture
corrections in 41.75 seconds:
`/private/tmp/eredu-reset-publication-native-check3.log`. This compile-only result
also includes the serial prepared-target residency fixtures. Subsequent actual
Metal Ring runs passed tensor (3.92 seconds), pipeline-streamed (3.68 seconds),
and tensor-host (3.92 seconds), each one test with no failures. Logs:
`/private/tmp/eredu-native-residency-readiness-ring-tensor.log`,
`/private/tmp/eredu-native-residency-readiness-ring-streamed.log`, and
`/private/tmp/eredu-native-residency-readiness-ring-host.log`.

This hook establishes the original source producer, not an end-to-end
authenticated original distributed generation request. Fresh fingerprinting
without prior discovery is covered separately by the architecture source test.

Build once without `DOCS_RS`, then execute the resulting backend test binary
with each exact name above (or use the corresponding filtered Cargo command):

```sh
cargo test -p eredu-backend-mlx --no-default-features --features metal --lib --no-run --offline
EREDU_TEST_RING_DEVICE=gpu cargo test -p eredu-backend-mlx --no-default-features --features metal --lib tests::distributed_pipeline_ring::ring_public_component_capture_qwen3_tensor --offline -- --exact --ignored --nocapture --test-threads=1
EREDU_TEST_RING_DEVICE=gpu cargo test -p eredu-backend-mlx --no-default-features --features metal --lib tests::distributed_pipeline_ring::ring_public_component_capture_qwen3_pipeline_streamed --offline -- --exact --ignored --nocapture --test-threads=1
EREDU_TEST_RING_DEVICE=gpu cargo test -p eredu-backend-mlx --no-default-features --features metal --lib tests::distributed_pipeline_ring::ring_public_component_capture_qwen3_tensor_host --offline -- --exact --ignored --nocapture --test-threads=1
```

The four-rank `ring_public_component_capture_qwen3_tensor_pipeline` remains the
additional combined-placement check; the two-rank set above passed. A singleton routed capture
test with declared peer tags cannot substitute for actual multi-process expert
parallel capture. Likewise, the CLI distributed control matrix does not select
a capture plan and cannot prove this source birth. These are validation scope
distinctions; this host's unexecuted Ring tests are not a hardware limitation.

## Serial prepared-target retirement oracle

The resident/host/disk parity helpers now return the exact pre-materialization
baseline alongside each loaded runtime. A new qualified target creates genuine
GPU/CPU stream registration and worker sources whose process lifetime can exceed
the runtime. Measuring only the earlier helper's root target therefore confused
later target source birth with a leaked model or request account.

The fixture measures pool usage before that target is realized, then immediately
before its `into_runtime` materialization. Both boundaries require zero unquoted
work, and the actual source-birth delta must be positive. All subsequent model,
request and escaped-output retirement comparisons use that returned target's
exact baseline. There is no constant allowance, post-generation warmup baseline,
ordinary stream promotion, or weakened output lifetime assertion. Actual reruns
are required to validate this corrected oracle; compile checks do not prove
retirement.

## Public distributed control follow-up

`eredu-cli/tests/k2_distributed_control.rs` now constructs its CPU backend with
`prepared_distributed_backend_on` before any ordinary Device/Stream. The same
backend retains the original execution/source owners through ordinary numerical
comparison and canonical controlled requests. This corrects a fixture that had
migrated its public requests while retaining ordinary stream construction, which
the original quote correctly rejects as unknown ownership.

All six dense/routed and resident/host/disk cases remain, with actual four- or
eight-rank local Ring execution, combined topology, paged state, deterministic
and sampled comparison, snapshot/restore/fork/exchange, cumulative copy spending
and cancellation assertions. No source qualification or capacity was changed.
Actual execution next exposed an ordinary reference request's still-live
unquoted owner at source compilation. The reference model and canonical model
now have separate lifecycles, while controlled trials retain one canonical
model. Genuine paid distributed/host/disk reset support is implemented; its
preparation/publication agreements retain the original source and metadata
account. Metadata alone does not establish completion of this matrix.
This matrix does not select capture and is separate from the component source
publication tests above.

The corrected fixture passes native-feature Rust metadata checking in 49.42
seconds, using the qualified compiler, disabled incremental compilation and
`DOCS_RS=1`. Log: `/private/tmp/contracts-cli-prepared-control-check.log`.
The actual run at this checkpoint reached controlled startup, then refused its first
checked prefill with `WorkspaceMetadata(Unqualified)`; see
`/private/tmp/eredu-native-cli-distributed-control-partition-reset.log`. That run
does not establish the full numerical or lifecycle matrix. Reuse the existing
native build target and SDK
environment, without `DOCS_RS`, rather than creating another native archive tree:

```sh
cargo test --offline -p eredu-cli --no-default-features --features mlx,metal --test k2_distributed_control --no-run
cargo test --offline -p eredu-cli --no-default-features --features mlx,metal --test k2_distributed_control k2_distributed_facade_control_matrix -- --exact --ignored --nocapture --test-threads=1
```

The test opens local loopback sockets and launches its worker test in each child
process. Its existing per-case deadline remains 120 seconds.

Focused reproducible commands (Rust 1.98.0, offline dependency cache):

```sh
cargo test -p eredu-checkpoint --lib --offline artifact::
cargo test -p eredu-core --lib --offline artifact::
cargo test -p eredu-architectures --lib --offline component_source::
cargo test -p eredu-architectures --lib --offline component_partition::
DOCS_RS=1 cargo check -p eredu-backend-mlx --no-default-features --features metal --tests --offline
cargo test -p eredu --no-default-features --test portable_facade --test backend_conformance --offline -- --test-threads=4
```

## Serial tracker policy follow-up

Actual serial runs now pass the exact per-target retirement baseline. Their next
refusal exposed a test-only fixed one-MiB submission tracker cap: the complete
source recipe requires 6,053,392 bytes for chat, 1,410,352 for plain generation and
1,403,888 for the controlled-prefill case. These parity fixtures now leave only
that optional override unset, selecting the existing exact recipe-derived tracker
capacity. They assert that the admitted original quote still owns its actual
record quota. Explicit one-short tracker refusal tests, graph limits, nonzero
KV/numerical comparison and exact retirement checks are unchanged. A subsequent
run exposed the analogous arbitrary four-MiB graph override, now removed from
these parity helpers so both quotas derive from the actual recipe. The separate
C1 component test still explicitly selects Record with its full selected recipe
capacity. Native reruns were still necessary at this checkpoint; the subsequent
serial residency and CPU numerical/custody results are recorded in the
[native work matrix](validation/bounded-followup-native-work-matrix-2026-09-18.json).
No quote or producer bound was reduced.

## Qwen-VL completed constructor source

The original direct dense composite partition now retains the completed Qwen-VL
constructor's exact configuration owner, TP and PP geometry owners, complete
local state declaration, global parameter description, execution graph, and
architecture fingerprint. Its cold workspace constructor aliases those owners
and uses the same text and vision static-module constructors as initial model
construction. Vision configuration is projected from the retained parent;
quotation does not clone nested configuration or derive local geometry again.

Metadata controls, module names, parameter declarations, graph copies and state
copies use the existing checked destination workers before their producers.
Borrowed parameter descriptions authenticate configuration and both geometry
owners; an independently equal replacement is not an admitted source. This
source remains private to architecture construction and the original prepared
partition handoff. It is not a public allocation-adoption or reconstruction API.

Focused source coverage passes two tests: all four TP2/PP2 ranks preserve the
exact configuration/geometry owners, global parameter groups, execution graph,
local state and group transports; independently equal replacement owners are
rejected. Checked cold construction also passes early, middle and final reached
funding refusal cuts, with actual account retirement after the final result
owner. Build: 64 seconds; tests: 0.03 seconds. Log:
`/private/tmp/contracts-qwen-vl-retained-source-tests.log`.
Combined partition-media driver and native validation were separate work at this
source checkpoint. The later TP2, PP2 and combined TP2/PP2 public image/tool cases
pass; see [native validation](bounded-followup-native.md#current-validation-scope).
The new publication currently applies to the existing direct dense executor;
this entry does not claim new routed composite workspace quotation support.

## Activation-free partition execution

The shared observer contract now distinguishes activation collection/intervention
interest from lifecycle and retained-media cut callbacks. No-op observers omit
activation paths and use the existing unobserved unit equations with the same
checked context, routed source loan and provider. Borrowed, error and cut
adapters preserve the fact. Group completion, dependency retention, transport,
policy completion and media-cut callbacks remain at their existing boundaries.
The decoder's shared component instrumentation also avoids path construction and
tensor clones for an observer without activation interest.

Architecture metadata checking passes in 18.71 seconds. A focused runtime test
passes cut traversal, original error identity and lifecycle forwarding through
borrowed/error adapters; log: `/private/tmp/contracts-unobserved-cut-test.log`.
Two architecture tests also pass: absent interest causes no activation callbacks
or tensor clones, while default interest preserves nested original/effective
paths and intervention values. Log:
`/private/tmp/contracts-unobserved-instrumentation-tests2.log` (39.67-second build).
A funded static pipeline stage diagnostic retained the original typed cause for
the next native CLI run. These changes closed a concrete unnecessary path producer;
that checkpoint alone did not resolve the CLI's first `Unqualified` refusal.
Later CPU producer corrections and passing Dense/Resident and Dense/Host execution
are recorded in [CLI validation](bounded-followup-cli.md#current-native-execution-scope).

Final no-default public suites after the observer capability change pass:
`backend_conformance` 126/126 (3.45 seconds) and `portable_facade` 38 passed,
two external-checkpoint tests ignored (15.04 seconds). Combined build: one minute
four seconds. Command used pinned Rust 1.98.0, offline dependencies, disabled
incremental compilation and `/private/tmp/eredu-core-driver-check`; log:
`/private/tmp/contracts-final-observer-public-neutral.log`. No refusal assertion
was loosened, and neither the full facade library nor the exhaustive schema
sweep was rerun for this bounded observer change. Native distributed validation
was separate from this portable result; the later public matrix is linked above.

## CPU YaRN source qualification

The four-rank K2 controlled CLI run localized its remaining first-prefill
`Unqualified` refusal to the actual unit-forward worker. Its retained fixture
uses YaRN on CPU. The CPU rotary source previously recognized only Default.
Further tracing established that K2 selects `InputProducts`, whereas the first
YaRN extension covered `Native` arithmetic. Both are real workers requiring
separate source facts; the first extension alone did not close K2. The fixture's
YaRN configuration is preserved.

The same CPU rotary quote now describes Default and YaRN full-width F32 native
arithmetic. YaRN uses the existing concentration multiply, batch/head flatten,
rank-three fallback, retained frequency denominator and final reshape. The
reciprocal is the native scalar Divide worker. Both variants count the real
Rust wrapper's per-batch slices and fallback calls, followed by its single
N-input concatenation. This also corrects Default's earlier single-batch census.
The retained frequency adds a traversal leaf, never a backing birth; actual
input arity determines the existing graph constructor's operand capacity.

The native control query now accepts rank three and four for the same named
fallback frames. Numerical execution and original-source authentication are
unchanged. The new original-scope test derives Graph, Record and Buffer storage
from the accepted recipe and checks singleton, two-way and six-way batches
against ordinary output and a scalar rotation oracle. It retains initial input
and frequencies while checking final invocation-quota retirement. The companion
C++ test checks rank-three supplied frequencies and escaped output custody.
Independent source review found no remaining census mismatch. Actual native
execution results below supersede that review as the validation evidence; the
six-case CLI matrix remains incomplete.

Combined backend library/test metadata passes with Metal, image and audio
features, including these new tests and the contemporaneous typed LayerNorm and
pipeline-media projection changes (35.53 seconds;
`/private/tmp/contracts-cpu-yarn-combined-metadata2.log`). The first fresh native
configure exposed two later patches whose header context still named the old
Default-only query; their context was updated. All 179 patches then applied
through the existing CMake patch worker to a freshly extracted pinned MLX
archive with verified SHA256
`8e74a0eb613861ce50c09402dd99dbc42b65a762e7fdd291caa39f611db978ec`.
Log: `/private/tmp/contracts-cpu-yarn-patch-series.log`. This was source and
metadata validation; actual execution followed in the later native work matrix.

The two focused C++ tests now **pass in fresh native processes** against the
current rebuilt archives: 78 assertions for finite Arange geometry plus positive
rank-three/four and negative-rank query immutability; 73 assertions for supplied
rank-three frequencies, scalar/ordinary numerical parity and independent
source/output custody. Both were selected with `--no-skip`; unrelated registered
tests were filtered out. Logs:
`/private/tmp/contracts-cpu-yarn-cpp-query.log` and
`/private/tmp/contracts-cpu-yarn-cpp-supplied.log`.

This validation compiled only the existing upstream doctest main,
`event_submission_tests.cpp` and `submission_record_quota_tests.cpp`, against
matching current headers, then linked the already rebuilt MLX/C/JACCL archives.
It did not change the main native CMake configuration or rebuild MLX. The
reproducible temporary project and exact commands are
`/private/tmp/eredu-cpu-yarn-patch-tests/CMakeLists.txt`, `build.sh` and `run.sh`;
its build log is `/private/tmp/contracts-cpu-yarn-cpp-harness-build.log`.
The recipe-funded Rust Native YaRN test subsequently failed during prepared
completion with a retained native Exception, despite passing cold census tests.
The failure was unresolved at that checkpoint; the test was extended to report
the retained Display cause and shape. Log:
`/private/tmp/eredu-native-producer-facts-cpu-yarn-native.log`.
The public CLI also still refused during its first unit forward at that stage.
Neither attempt counts as successful execution. The later capture-peak correction
and passing Native YaRN run are recorded in the native work matrix linked above.


## K2 CPU source closure checkpoint

The actual `InputProducts` rotary quote now follows the existing elementwise
worker: integer absolute positions followed by F32 casting, its uploaded inverse
frequency source, one shared amplitude scalar applied to cosine and sine, full
rotary and half slices, four products, two combinations and concatenation. Exact
retained strides select whether flattening aliases or copies. The inverse
frequency is a traversal leaf, never a new birth. Backend library/test metadata
passed with the dense/transposed, large-integer-offset original-scope test before
its first native execution. The later InputProducts fixture passes ordinary/paid
parity, source-derived scalar accuracy and custody; see
[native numerical evidence](bounded-followup-native.md#numerical-and-native-c-evidence).

The complete K2 path also selects grouped RMS normalization before rotary and
CPU paged blockwise attention afterward. Both previously lacked CPU source
facts at that earlier checkpoint. Grouped RMS and CPU blockwise sources are now
implemented for their actual selected workers, as recorded below; Metal source
estimates were not used to qualify the CPU worker. A paid fixed paged
source diagnostic now identifies whether a key or value lacks physical floating
representation while preserving the original typed refusal and funding custody.

Grouped RMS combined backend library/test metadata now passes (56.04 seconds;
`/private/tmp/contracts-grouped-rms-metadata.log`). Independent review caught and
closed two concrete issues before this checkpoint: exact physical-stride slice
length and the added group axis in the graph's maximum-rank census. At that metadata checkpoint the cold
27-case and native nine-case tests compiled; subsequent execution is recorded below.

The grouped RMS tests now pass actual execution: the 27-case cold source matrix
and the nine original CPU group/scale cases passed in the parent's current
backend run. InputProducts cold stride/coordinate tests passed too, but its native
fixture at this checkpoint failed while validating a retained source leaf. That
failure and Native YaRN's six-batch capture-limit failure were separate open
checks, subsequently closed by the native results linked above.

## CPU paged blockwise source checkpoint

The selected F32 CPU recurrence now consumes the same neutral stage descriptor
and unchanged native accumulator. Begin retains the real query/mask aliases;
each Accumulate prices its actual integer causal/window/prefix mask, optional
caller mask/bias/cap/sinks, grouped-head views and copies, matrix products,
normalization or rounded-probability pass, and three or one completed outputs.
Finish preserves the actual InputScores alias or fused division. The CPU reducer
counts one nested completion per accepted page and retains the existing maximum
three-root settlement contract. No cache manager, block or scan authority comes
from these scalar facts.

The first InputScores query product uses exact retained matrix strides to price
an actual compact copy. Logical layout facts canonicalize singleton-axis
strides, so such matrix axes retain the proved possible-one-copy envelope. This
covers an actual transposed one-position query that reports row-contiguous while
its raw matrix stride still makes the native worker copy. Unknown non-singleton
stride facts continue to refuse. The shared CPU row-Max query authenticates
F32, readable backing, exact final-axis/shape and contiguous row strides against
the actual Reduce primitive; it retains the unchanged SIMD/NaN/tail order.
Its controls cover both multiple-row and one-row all-reduce dispatch.

Validation at this checkpoint:

- All **180 MLX patches** apply to a fresh verified pinned archive through the
  existing CMake worker: `/private/tmp/contracts-cpu-blockwise-patch-series.log`.
- Backend library/all-tests metadata passes in **37.56 seconds**:
  `/private/tmp/contracts-cpu-blockwise-metadata3.log`.
- The changed reduction query/dispatcher and complete C++ test translation unit
  pass syntax checks against that patched source:
  `/private/tmp/contracts-cpu-blockwise-cpp-syntax2.log`.
- Independent review closed the actual strided-query and singleton raw-stride
  findings before that checkpoint. The Rust cold/native filters below were
  awaiting a rebuilt test binary then; both subsequently passed in the native
  work matrix linked above.

The Rust cold filter is
`cpu_paged_stages_preserve_exact_rows_outputs_and_nested_completions`; it covers
48 combinations of Fused/InputScores, absent/Boolean/additive masks, ordinary/GQA
heads, singleton/multiple query rows and dense/transposed queries, with uneven
pages, sinks, cap and bias. The ignored native filter is
`original_cpu_paged_prefill_and_decode_match_ordinary_nonzero_state`; it uses a
real prepared CPU factory, installed paged manager and original token source,
compares prefill plus three decode decisions with ordinary execution, and checks
nonzero retained state and retirement to the actual target-source baseline.
It does not fabricate a test-only cache or block authority.

The three C++ filters are `CPU paged row maximum source preserves exact geometry
and query refusal`, `CPU paged row maxima retain ordinary SIMD values and escaped
source custody`, and `CPU paged score rows preserve singleton transposed query
copies and original custody`. The latter directly exercises the raw singleton
stride distinction. All three now pass against the fresh 180-patch main native
archive, in separate authorized processes: **22 + 100 + 38 = 160 assertions**.
The row-Max case includes both multiple rows and the one-row all-reduce branch;
the singleton query case confirms the real copy before checking numerical
parity and escaped-owner retirement. Logs are
`/private/tmp/contracts-cpu-blockwise-cpp-{query,maximum,singleton}.log`.
The reproducible lightweight harness is
`/private/tmp/eredu-cpu-blockwise-patch-tests/{build.sh,run.sh}`; set
`EREDU_NATIVE_BUILD` to the completed main native `out/build` directory and run
each script with `sh`. It compiles only existing test translation units and
links the current archives, without altering the main CMake configuration or
building another MLX archive. The validated executable's SHA256 is
`3e9976c2652d617dbc89ff7340688d0d825559ac38a342de4f8f811e16a20c94`. This new CPU
stage source currently establishes F32 physical inputs/output; it does not claim
new half-precision paged CPU qualification or completion of the six-case public
CLI matrix.


The subsequent native run passed Native YaRN after its real six-input
concatenate capture peak was corrected. The first CPU blockwise cold run reached
a complete reducer source but exposed an obsolete `inspect_cpu_equations`
restriction against every internal nested completion. That helper now combines
the reducer's actual nested occurrences with caller capture reads before
constructing graph and record capacities; it preserves the larger actual nested
root requirement. The subsequent source-settlement binary passes the 48-case cold filter against this correction.
The paged native fixture initially stopped after its ordinary trial because its
baseline included the CPU execution wrapper's independently retiring account.
It now measures that exact wrapper's `original_bytes()` before model loading
and the independently funded selected-context copy's `original_bytes()`;
both local wrapper accounts retire while process stream/worker registrations
survive. The next actual run identified the second account after the first
correction (3,217 bytes in that build). The fixture excludes only these actual
owners from the post-backend baseline. This is a fixture
correction, not a quota allowance. The original trial was still unexecuted at
that checkpoint; it subsequently passed prefill, multiple decode steps, exact
nonzero state parity and retirement.

The fresh public K2 run still stopped before numerical execution because a pure
range index lost its key representation. The sole neutral `Tensor::index`
producer previously emitted only the number of removed axes. Its Full/Range
branch now retains the real normalized starts, ends and unit strides in the
existing StaticSlice event; At retains the axis-removal event. Native indexing
is unchanged. Static-slice accounting includes its actual basic-index and Mlx
frontend controls, including source-derived SmallVec spill storage when needed.
CPU does not accept a legacy rank-count-only Index event. All 211 neutral NN
library tests pass, including negative/empty intervals, axis removal, and refusal
at every reached coordinate-metadata producer:
`/private/tmp/contracts-index-coordinates-nn-all.log`. The new cold native filter
is `cpu_paged_range_index_retains_key_scalar_strides_and_exact_source_backing`.
It subsequently passed in the native work matrix; the separate CLI stages are
maintained in [CLI validation](bounded-followup-cli.md).

The affected runtime working-memory suite also passes **1,085/1,085** after
coordinate retention (`/private/tmp/contracts-index-coordinates-runtime-tests.log`).
The architecture workspace-estimation run initially reached **11/13**; it exposed a
DeepSeek V3 disabled-observer assumption and a neutral fixture's missing F32
representation declaration. Both were corrected, and the complete 13-case suite
subsequently passed, as recorded in
[ordinary-frontdoor evidence](bounded-followup-ordinary-frontdoors.md).
The next actual K2
run passes the key-source boundary but refuses an AllReduce because its floating input representation is absent
(`/private/tmp/eredu-native-cli-distributed-control-canonical-slices.log`).
The range recipe recognizer has also migrated to exact StaticSlice coordinates;
that execution helper does not itself explain the subsequent collective scalar
refusal. New cold coverage follows the paged output through transpose, reshape
and row projection. The public matrix was incomplete at that checkpoint; later
CPU Host producer corrections closed this first-prefill refusal. That producer result alone did not establish the CLI matrix; the final six-case
pass is recorded separately in [CLI validation](bounded-followup-cli.md).

The subsequent source-settlement native binary passes the CPU paged cold
source filter and exact range-source filter. The paged numerical fixture's
ordinary trial stopped at final retirement, which identified the second local
selected-context account described above; its original trial had not run yet.
The later native matrix passes the post-attention projection filter, exact
two-wrapper retirement assertion and original paged numerical fixture.

Aborted unit quotes now retain the first incomplete equation's descriptor
through the same funded metadata producer. The private error enclosure is
funded before calling the unit; an existing funding/resource refusal bypasses
inspection. A reached diagnostic refusal retains both itself and the original
typed failure without another account callback. Only text and scalar facts
escape, with actual funding custody. Five focused tests pass in
`/private/tmp/contracts-unit-failure-diagnostic-final-tests3.log`, including every
reached inspection refusal and priority for a missing floating result over an
earlier unpriced zero-output effect; these diagnostics
do not change quotation, admission or execution policy.


The retained-waves K2 run still refuses that collective. Its initial diagnostic
selected an earlier `HostStoreFloating` effect. Source inspection establishes a
real CPU qualification gap: the ordinary Host transfer workers existed, but
original constructors admitted only GPU and CPU plans omitted both store/load.
The shared CPU General-copy bank now describes those same workers; the original
constructor keeps exact Host policy, backing identity, prepared stream and nested
completion checks. Store binds its independently accepted Host destination and
can compact once; load owns one new device output and retains an existing Host
leaf. The CPU reducer separates that leaf's descriptor seed from backing births,
and retains internal store roots and every actual nested completion.

The complete **181-patch** sequence applies to the verified pinned archive
(`/private/tmp/contracts-cpu-host-patch-series.log`); all three changed C++
production units and the expanded test translation unit pass syntax checks.
New cold coverage joins store/load through paged attention to the actual
Projection event. A separate native fixture forces real Host demotion/promotion
under a two-page policy and preserves ordinary/original token, state and exact
retirement comparisons. These numerical and public CLI runs were pending at that
source checkpoint. The later original CPU Host spill/reload fixture passes at the
unchanged 256-byte two-page limit, and Dense/Host CLI controls pass both sampling
modes. Their actual execution records, rather than source inspection, establish
those results.

The combined backend library/all-test metadata check passes in **37.30s**
(`/private/tmp/eredu-native-paid-sum-wave-metadata3.log`), including the Host
source, spill fixture, retained sum wave and canonical NN error surface. An
independent source review found no mismatch in native worker populations,
Host/stream guards, hidden descriptor versus backing accounting, or completion
root custody. Device execution was still required after that metadata check;
the subsequent passing results are linked above.

The current CPU Host patch also passes its actual C++ regression: **1 case,
265 assertions**, linked against the fresh `safemlx-sys-257004b9a0b09799`
source and archives (`/private/tmp/eredu-current-cpu-host-cpp-test.log`). The
matching `safemlx` Metal/SafeTensors build passes in **29.12s**. This closes
native Host transfer/query/foreign-backing checks. Later full-model spill/reload
and Dense/Host CLI results are recorded separately in the native and CLI guides;
this C++ result alone does not establish either.
