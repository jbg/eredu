# Sampling control consolidation inventory

The existing shared runtime validator checks temperature and RNG compatibility.
Native installation prepares an optional replacement key, proves completion, and
publishes temperature/key together. Controlled and detached consumers must use
this same operation. The previous `mechanism_parts` route exposed broader mutable
state and revoked the original source policy just to change sampling. Core now
owns the closed request/facts/error/backend contract; runtime retains validation,
and a completed-boundary core guard calls the backend without exposing state.
The former public installer taking a validated action is removed. MLX keeps one
private installer after shared validation.

The former funded installation rejected because the immutable text quote bound
both exact temperature and per-attempt sampling programs. Each prediction issues
five finite roles, and the sampling roles carry the original native graph/record
quotas, source storage and completion equations. A temperature change crossing
greedy/stochastic modes changes that real graph. Reseeding also creates a new key
while the old key remains live. Simply changing temperature or invoking the
unquoted key constructor would invalidate those facts.

The canonical extension traces the actual retained sampler using the existing
workspace sampling worker, preserves history and adaptive counters, authenticates
the original request and remaining attempt interval, and admits replacement and
future native work before publication. Model equations, capture/controller
admission, original output allowance and spent attempts remain the same source.
Prior sampling programs and escaped native owners keep their funding until actual
completion and retirement. Failure must leave the installed logical state intact;
an admitted failed construction cannot replay its finite preparation permission.
No second generation engine, snapshot-and-restart workaround, or payer-selected
sampling algorithm is introduced.

The native implementation now retains the exact original score input descriptor
and moves the sampling rows out of the existing resident recorder when quoting a
replacement. The same graph/record reducers and physical population worker serve
initial and revised programs. Genuine sampling spans carry these observed phases
through the existing protected native-storage admission. The original run issues
model, validation and scalar roles; the committed extension issues sampling and
sampling-event roles for that same active step.

The existing completion owner retains a second publication work account for
replacement token and RNG roots. It publishes and certifies that account only at
the same successful settlement cut as model work. Reseeding uses the same prepared
native recovery worker as startup, without constructing another sampler or
copying history. Temperature, key and program are moved into their closed slots
after successful native preparation and extension commit. Earlier programs and
escaped arrays retain their actual accounts. Exact no-op requests authenticate
the completed boundary but introduce no new program.

Core controller projection and neutral extension origin/account/refusal tests
pass. At the initial native checkpoint, the public sampling override, reseed and
snapshot assertions passed with a 64 MiB test-thread stack. A default 2 MiB run
overflowed during initial capture planning, before the override call.
Symbolication identified the enlarged inline optional sampling revision across
startup quote transports. That revision now uses separately paid closed Box
ownership, whose allocation retires before its funding accounts. Later public
ordinary tool/capture branch tests and the distributed CLI Dense/Resident,
Dense/Host and Dense/Disk control cases pass, including both sampling modes; see
[native validation](bounded-followup-native.md#current-validation-scope) and
[CLI validation](bounded-followup-cli.md#current-native-execution-scope).
Those native results use an explicit 64 MiB Rust test-thread stack. The
qualified canonical-cell receipt checkpoint passes all seven prepared-chat cases
and all six paged control cases at their unchanged limits. Earlier ordinary runs on the
default 2 MiB stack abort: the first test-only phase split allows initial startup,
then the first checkpoint fork overflows across finite large fixture/shared resume debug
frames. A second test-only split separates checkpoint creation from serial
fork/exchange/restore. Its metadata check passes in 48.17 seconds, with independent
equivalence review. The later native diagnostic run passes the preceding
capture/restore phases, then overflows during `sampling::check_recorded_child`
at `LoadedModel::start_controlled_chat` (exit -6, 9.279 seconds process wall time).
The external probe identifies `sampling::emit` in the finite unoptimized startup
chain. No further default-stack repair is planned. Exact diagnostics are
`/private/tmp/eredu-native-source-receipt-default-stack.json`,
`/private/tmp/eredu-native-disk-custody-probe-default-stack.json`, and
`/private/tmp/native-default-stack-diagnosis.json`. The 64 MiB qualification
therefore remains explicit; passing those public cases does not complete the
separate distributed CLI matrix. Source retention and local bank composition are
implemented and pass the 29-case source-contract checkpoint; subsequent CPU source
qualification is recorded in the
[native work matrix](validation/bounded-followup-native-work-matrix-2026-09-18.json).
Routed CLI acceptance remains pending in [CLI validation](bounded-followup-cli.md).

Child resume prepares the same sampling extension after the new copied sampler's
original readiness and before native state placement. The private endpoint
checks the pending opening, exact source pair and fresh core context. A failed
preparation leaves the installed parent untouched; the new key and program
retain the extension's actual account. The neutral filter
`control::snapshots::facade_branches_keep_partial_text_identity_siblings_and_budgets_isolated`
exercises explicit child temperature and reseed options. The native speculative
fixture `speculative::native_speculative_forks_force_tokens_and_apply_independent_target_and_draft_tensor_edits`
instead changes sampling after exchanging a child into the active slot. That is
a distinct endpoint. The public native ordinary prepared-chat fixture now also
passes a resume-time change through `GenerationBranchOptions::sampling`:
temperature 0.5 and seed 711, checked branch provenance and installed facts,
snapshot/restore of the changed child, manual/uninterrupted semantic parity, and
preservation of the parent's original continuation. Its existing default-restore
assertions remain intact. Required and Auto both run at the unchanged 8 GiB
capacity with one snapshot and one branch, preserving cumulative copy spending.

The exact filter is
`ordinary_tool_capture_snapshot_restore_and_fork_preserve_committed_semantics`;
the historical canonical-cell receipt pass took 2.84 test-harness seconds in
`/private/tmp/eredu-native-canonical-cell-receipt-ordinary.log`. Its artifact
provenance is `/private/tmp/eredu-public-canonical-cell-receipt-build-record.json`;
the binary digest and scripted-decoder scope are recorded in
[public conformance](bounded-followup-public-conformance.md#child-resume-time-sampling-native-execution).
This result includes shared physical receipts for lazy Host sources and
persistent canonical CPU arrays, plus both fixture phase splits, with the explicit
64 MiB test stack. The earlier 2.89-second source receipt pass remains in
`/private/tmp/eredu-native-source-receipt-ordinary.log`, and the 2.81-second pass in
`/private/tmp/eredu-native-existing-physical-ordinary.log`. The separate
second-split default-stack failure does not invalidate this 64 MiB result or
establish full execution with the default debug stack.
