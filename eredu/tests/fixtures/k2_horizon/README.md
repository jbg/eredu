Numerical reference copied from `eredu-architectures/tests/fixtures/k2_horizon/reference.json`.
Regenerate with that directory's `generate_reference.py`, then refresh this copy.
The local copy keeps published crate tests independent of sibling checkouts.
