diff --git a/mlx/record_quota.cpp b/mlx/record_quota.cpp
--- a/mlx/record_quota.cpp
+++ b/mlx/record_quota.cpp
@@ -1,5 +1,7 @@
 #include "mlx/record_quota.h"
 
+#include <cstdio>
+#include <cstdlib>
 #include <algorithm>
 #include <cassert>
 #include <cstring>
@@ -197,7 +199,12 @@
     result = c.result;
     // No scratch pointer is an owning reference or consulted outside this loan.
   }
-  if (!result) throw RecordQuotaError(QuotaFailure::exhausted);
+  if (!result) {
+    if (std::getenv("EREDU_RECORD_CAPACITY_TRACE"))
+      std::fprintf(stderr, "RECORD_CAPACITY_REFUSAL source=arena requested=%zu alignment=%zu capacity=%zu occupied=%zu\n",
+          requested, align, capacity_, occupied_bytes());
+    throw RecordQuotaError(QuotaFailure::exhausted);
+  }
   return result;
 }
 
diff --git a/mlx/eval_storage.h b/mlx/eval_storage.h
--- a/mlx/eval_storage.h
+++ b/mlx/eval_storage.h
@@ -1,5 +1,7 @@
 #pragma once
 
+#include <cstdio>
+#include <cstdlib>
 #include <cstdint>
 #include <utility>
 
@@ -15,8 +17,12 @@
   size_t limit_;
 
   void room() const {
-    if (values_.size() == limit_)
+    if (values_.size() == limit_) {
+      if (std::getenv("EREDU_RECORD_CAPACITY_TRACE"))
+        std::fprintf(stderr, "RECORD_CAPACITY_REFUSAL source=fixed-vector element_bytes=%zu size=%zu limit=%zu type=%s\n",
+            sizeof(T), values_.size(), limit_, __PRETTY_FUNCTION__);
       throw submission::RecordQuotaError(submission::QuotaFailure::exhausted);
+    }
   }
 
  public:
diff --git a/mlx/transforms.cpp b/mlx/transforms.cpp
--- a/mlx/transforms.cpp
+++ b/mlx/transforms.cpp
@@ -1,3 +1,5 @@
+#include <cstdio>
+#include <cstdlib>
 #include <typeinfo>
 // Copyright © 2023-2024 Apple Inc.
 #include <algorithm>
@@ -522,8 +524,13 @@
       if (idx < a.inputs().size()) {
         // Add an input, and continue
         if constexpr (Fixed) {
-          if (visited_edges == limits->input_edges)
+          if (visited_edges == limits->input_edges) {
+            if (std::getenv("EREDU_RECORD_CAPACITY_TRACE"))
+              std::fprintf(stderr, "RECORD_CAPACITY_REFUSAL source=input-edges visited=%zu limit=%zu arrays=%zu tape=%zu roots=%zu current_inputs=%zu current_index=%zu\n",
+                  visited_edges, limits->input_edges, limits->array_nodes,
+                  limits->tape_entries, limits->root_count, a.inputs().size(), idx);
             throw submission::RecordQuotaError(submission::QuotaFailure::exhausted);
+          }
           ++visited_edges;
         }
         auto& in = a.inputs()[idx++];
