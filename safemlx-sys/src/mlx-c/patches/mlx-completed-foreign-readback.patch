diff --git a/mlx/allocator.h b/mlx/allocator.h
--- a/mlx/allocator.h
+++ b/mlx/allocator.h
@@ -109,6 +109,9 @@
   void* raw_ptr();
 
   // Synchronous completed-storage export. Never relocates this backing.
+  // Readable extent of a retained foreign host buffer, independent of allocator
+  // capacity, physical-root identity and admission attribution. No owner is made.
+  bool foreign_host_readback_extent(size_t& bytes) const noexcept;
   int copy_to_host(void* destination, size_t offset, size_t bytes) const noexcept;
 
   // Get the buffer pointer from the buffer
diff --git a/mlx/backend/metal/allocator.cpp b/mlx/backend/metal/allocator.cpp
--- a/mlx/backend/metal/allocator.cpp
+++ b/mlx/backend/metal/allocator.cpp
@@ -24,6 +24,13 @@
 
 Allocator& allocator() {
   return metal::allocator();
+}
+
+bool Buffer::foreign_host_readback_extent(size_t& bytes) const noexcept {
+  auto* buffer = static_cast<MTL::Buffer*>(ptr_);
+  if (!buffer || buffer->storageMode() == MTL::StorageModePrivate || !buffer->contents()) return false;
+  bytes = buffer->length();
+  return true;
 }
 
 int Buffer::copy_to_host(void* destination, size_t offset, size_t bytes) const noexcept {
diff --git a/mlx/backend/no_gpu/allocator.cpp b/mlx/backend/no_gpu/allocator.cpp
--- a/mlx/backend/no_gpu/allocator.cpp
+++ b/mlx/backend/no_gpu/allocator.cpp
@@ -413,6 +413,11 @@
   return common_allocator();
 }
 
+bool Buffer::foreign_host_readback_extent(size_t&) const noexcept {
+  // This backend supplies no separate foreign-host readable-extent witness.
+  return false;
+}
+
 int Buffer::copy_to_host(void* destination, size_t offset, size_t bytes) const noexcept {
   if (!bytes) return 0;
   if (!destination || !ptr_) return -1;
diff --git a/mlx/backend/cuda/allocator.cpp b/mlx/backend/cuda/allocator.cpp
--- a/mlx/backend/cuda/allocator.cpp
+++ b/mlx/backend/cuda/allocator.cpp
@@ -439,6 +439,11 @@
   return cu::allocator();
 }
 
+bool Buffer::foreign_host_readback_extent(size_t&) const noexcept {
+  // This backend supplies no separate foreign-host readable-extent witness.
+  return false;
+}
+
 int Buffer::copy_to_host(void* destination, size_t offset, size_t bytes) const noexcept {
   if (!bytes) return 0;
   const auto* source = static_cast<const cu::CudaBuffer*>(ptr_);
