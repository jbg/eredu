diff --git a/mlx/backend/cpu/alias_storage.cpp b/mlx/backend/cpu/alias_storage.cpp
--- a/mlx/backend/cpu/alias_storage.cpp
+++ b/mlx/backend/cpu/alias_storage.cpp
@@ -20,6 +20,7 @@
       (value.dtype()==uint8&&value.itemsize()==sizeof(uint8_t))||
       (value.dtype()==int8&&value.itemsize()==sizeof(int8_t))||
       (value.dtype()==int32&&value.itemsize()==4)||
+      (value.dtype()==int64&&value.itemsize()==sizeof(int64_t))||
       (value.dtype()==float32&&value.itemsize()==4)||
       (value.dtype()==float16&&value.itemsize()==2)||
       (value.dtype()==bfloat16&&value.itemsize()==2)||
diff --git a/mlx/backend/cpu/concatenate_storage.cpp b/mlx/backend/cpu/concatenate_storage.cpp
--- a/mlx/backend/cpu/concatenate_storage.cpp
+++ b/mlx/backend/cpu/concatenate_storage.cpp
@@ -20,7 +20,8 @@
 }
 bool concatenate_many_eval_layout(Dtype dtype,size_t rank,size_t inputs,size_t elements,bool tracer,CopyEvalStorage& out) noexcept {
   if(!((dtype==float32&&dtype.size()==4)||(dtype==float16&&dtype.size()==2)||
-      (dtype==bfloat16&&dtype.size()==2)||(dtype==uint8&&dtype.size()==1)||(dtype==int32&&dtype.size()==4)||(dtype==uint32&&dtype.size()==4))||rank<1||rank>4||
+      (dtype==bfloat16&&dtype.size()==2)||(dtype==uint8&&dtype.size()==1)||(dtype==int32&&dtype.size()==4)||(dtype==uint32&&dtype.size()==4)||
+      (dtype==int64&&dtype.size()==sizeof(int64_t)))||rank<1||rank>4||
       inputs<2||inputs>size_t(INT_MAX)||elements>size_t(INT_MAX)||inputs>(SIZE_MAX-1)/2)return false;
   const size_t weak_copies=inputs*2;
   CopyEvalStorage value;
diff --git a/mlx/backend/cpu/reshape_storage.cpp b/mlx/backend/cpu/reshape_storage.cpp
--- a/mlx/backend/cpu/reshape_storage.cpp
+++ b/mlx/backend/cpu/reshape_storage.cpp
@@ -96,6 +96,7 @@
         (input.dtype()==uint8&&input.itemsize()==sizeof(uint8_t))||
         (input.dtype()==uint32&&input.itemsize()==sizeof(uint32_t))||
         (input.dtype()==int32&&input.itemsize()==sizeof(int32_t))||
+        (input.dtype()==int64&&input.itemsize()==sizeof(int64_t))||
         (input.dtype()==float16&&input.itemsize()==2)||
         (input.dtype()==bfloat16&&input.itemsize()==2)))return false;
   size_t source_elements=0;
