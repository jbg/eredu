diff --git a/mlx/backend/cpu/alias_storage.cpp b/mlx/backend/cpu/alias_storage.cpp
--- a/mlx/backend/cpu/alias_storage.cpp
+++ b/mlx/backend/cpu/alias_storage.cpp
@@ -16,6 +16,7 @@
 }
 bool canonical_dtype(const array& value) noexcept {
   return (value.dtype()==bool_&&value.itemsize()==sizeof(bool))||
+      (value.dtype()==uint8&&value.itemsize()==sizeof(uint8_t))||
       (value.dtype()==int32&&value.itemsize()==4)||
       (value.dtype()==float32&&value.itemsize()==4)||
       (value.dtype()==float16&&value.itemsize()==2)||
diff --git a/mlx/backend/cpu/reshape_storage.cpp b/mlx/backend/cpu/reshape_storage.cpp
--- a/mlx/backend/cpu/reshape_storage.cpp
+++ b/mlx/backend/cpu/reshape_storage.cpp
@@ -33,16 +33,29 @@
   if(controls>SIZE_MAX-value.named_control_bytes)return false;
   value.named_control_bytes+=controls;out=value;return true;
 }
+bool reshape_copy_eval_layout(size_t rank,size_t output_rank,bool tracer,CopyEvalStorage& out) noexcept {
+  if(rank>5||output_rank>5||Shape::inline_capacity<5||Strides::inline_capacity<5)return false;
+  CopyEvalStorage value;
+  if(!copy_eval_layout(std::max(rank,output_rank),1,true,tracer,value))return false;
+  const size_t controls=fixed_reshape_plan_control_bytes()+sizeof(FixedReshapePlan)+sizeof(value)+sizeof(out)+
+      sizeof(rank)+sizeof(output_rank)+sizeof(tracer)+sizeof(std::pair<bool,Strides>)+sizeof(Strides)+
+      sizeof(const array*)*2+sizeof(bool)+sizeof(const int*)*2+sizeof(const int64_t*)+
+      sizeof(size_t)*2+sizeof(bool)+sizeof(CopyEvalStorage*);
+  if(controls>SIZE_MAX-value.named_control_bytes)return false;
+  value.named_control_bytes+=controls;out=value;return true;
+}
 bool reshape_eval_layout(const int* source_shape,const int64_t* source_strides,
     size_t rank,const int* output_shape,size_t output_rank,bool tracer,CopyEvalStorage& out) noexcept {
   FixedReshapePlan plan;
   if(!fixed_reshape_plan(source_shape,source_strides,rank,false,output_shape,output_rank,plan))return false;
+  if(plan.copy) {
+    // This source includes the fixed planner/caller controls as well as the
+    // shared General copy. The scalar cold query and actual copy branch use
+    // the same complete census; do not append that planner a second time.
+    return reshape_copy_eval_layout(rank,output_rank,tracer,out);
+  }
   CopyEvalStorage value;
-  if(plan.copy) {
-    // One actual output Data and the existing General-copy task; no additional
-    // destination shell because the Reshape output already owns its descriptor.
-    if(!copy_eval_layout(std::max(rank,output_rank),1,true,tracer,value))return false;
-  } else if(!reshape_alias_eval_layout(rank,output_rank,tracer,value))return false;
+  if(!reshape_alias_eval_layout(rank,output_rank,tracer,value))return false;
   const size_t controls=fixed_reshape_plan_control_bytes()+sizeof(plan)+sizeof(value)+sizeof(out)+
       sizeof(source_shape)+sizeof(source_strides)+sizeof(rank)+sizeof(output_shape)+sizeof(output_rank)+sizeof(tracer)+
       sizeof(std::pair<bool,Strides>)+sizeof(Strides)+sizeof(const array*)*2+sizeof(bool);
@@ -57,6 +70,7 @@
   if(rank>5||output_rank>5||shape.size()!=output_rank||
       input.size()==0||input.dtype()!=output.dtype()||input.itemsize()!=output.itemsize()||
       !((input.dtype()==float32&&input.itemsize()==sizeof(float))||
+        (input.dtype()==uint8&&input.itemsize()==sizeof(uint8_t))||
         (input.dtype()==uint32&&input.itemsize()==sizeof(uint32_t))||
         (input.dtype()==int32&&input.itemsize()==sizeof(int32_t))||
         (input.dtype()==float16&&input.itemsize()==2)||
diff --git a/mlx/backend/cpu/reshape_storage.h b/mlx/backend/cpu/reshape_storage.h
--- a/mlx/backend/cpu/reshape_storage.h
+++ b/mlx/backend/cpu/reshape_storage.h
@@ -2,6 +2,7 @@
 #include "mlx/backend/cpu/copy_storage.h"
 namespace mlx::core::cpu {
 MLX_API bool reshape_alias_eval_layout(size_t rank,size_t output_rank,bool tracer,CopyEvalStorage&) noexcept;
+MLX_API bool reshape_copy_eval_layout(size_t rank,size_t output_rank,bool tracer,CopyEvalStorage&) noexcept;
 MLX_API bool reshape_eval_layout(const int* source_shape,const int64_t* source_strides,
     size_t rank,const int* output_shape,size_t output_rank,bool tracer,CopyEvalStorage&) noexcept;
 MLX_API bool is_reshape_alias_eval_primitive(const array&) noexcept;
diff --git a/mlx/backend/cpu/concatenate_storage.cpp b/mlx/backend/cpu/concatenate_storage.cpp
--- a/mlx/backend/cpu/concatenate_storage.cpp
+++ b/mlx/backend/cpu/concatenate_storage.cpp
@@ -19,7 +19,7 @@
 }
 }
 bool concatenate_eval_layout(Dtype dtype,size_t rank,size_t left,size_t right,bool tracer,CopyEvalStorage& out) noexcept {
-  if(((dtype!=float32&&dtype!=bfloat16)||(dtype==float32&&dtype.size()!=4)||(dtype==bfloat16&&dtype.size()!=2))||rank<1||rank>4||!left||!right||
+  if(((dtype!=float32&&dtype!=bfloat16&&dtype!=uint8)||(dtype==float32&&dtype.size()!=4)||(dtype==bfloat16&&dtype.size()!=2)||(dtype==uint8&&dtype.size()!=1))||rank<1||rank>4||!left||!right||
       left>size_t(INT_MAX)||right>size_t(INT_MAX)-left)return false;
   CopyEvalStorage value;
   // One real Eval/cleanup over two inputs. Its two inplace jobs below have no
