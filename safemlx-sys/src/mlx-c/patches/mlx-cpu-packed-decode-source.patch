diff --git a/mlx/backend/cpu/binary.h b/mlx/backend/cpu/binary.h
--- a/mlx/backend/cpu/binary.h
+++ b/mlx/backend/cpu/binary.h
@@ -478,6 +478,45 @@
 }
 
 template <typename Op>
+struct BinaryIntTask {
+  array a, b, out;
+  BinaryOpType bopt;
+  void operator()() {
+    switch (out.dtype()) {
+      case bool_:
+        binary_op<bool, Op>(a, b, out, bopt);
+      case uint8:
+        binary_op<uint8_t, Op>(a, b, out, bopt);
+        break;
+      case uint16:
+        binary_op<uint16_t, Op>(a, b, out, bopt);
+        break;
+      case uint32:
+        binary_op<uint32_t, Op>(a, b, out, bopt);
+        break;
+      case uint64:
+        binary_op<uint64_t, Op>(a, b, out, bopt);
+        break;
+      case int8:
+        binary_op<int8_t, Op>(a, b, out, bopt);
+        break;
+      case int16:
+        binary_op<int16_t, Op>(a, b, out, bopt);
+        break;
+      case int32:
+        binary_op<int32_t, Op>(a, b, out, bopt);
+        break;
+      case int64:
+        binary_op<int64_t, Op>(a, b, out, bopt);
+        break;
+      default:
+        throw std::runtime_error("[binary_int] Type not supported");
+        break;
+    }
+  }
+};
+
+template <typename Op>
 void binary_int_op_cpu(
     const array& a,
     const array& b,
@@ -486,47 +525,12 @@
     Stream stream) {
   auto bopt = get_binary_op_type(a, b);
   set_binary_op_output_data(a, b, out, bopt);
-
   auto& encoder = cpu::get_command_encoder(stream);
   encoder.set_input_array(a);
   encoder.set_input_array(b);
   encoder.set_output_array(out);
-  encoder.dispatch([a = array::unsafe_weak_copy(a),
-                    b = array::unsafe_weak_copy(b),
-                    out = array::unsafe_weak_copy(out),
-                    bopt]() mutable {
-    switch (out.dtype()) {
-      case bool_:
-        binary_op<bool, Op>(a, b, out, bopt);
-      case uint8:
-        binary_op<uint8_t, Op>(a, b, out, bopt);
-        break;
-      case uint16:
-        binary_op<uint16_t, Op>(a, b, out, bopt);
-        break;
-      case uint32:
-        binary_op<uint32_t, Op>(a, b, out, bopt);
-        break;
-      case uint64:
-        binary_op<uint64_t, Op>(a, b, out, bopt);
-        break;
-      case int8:
-        binary_op<int8_t, Op>(a, b, out, bopt);
-        break;
-      case int16:
-        binary_op<int16_t, Op>(a, b, out, bopt);
-        break;
-      case int32:
-        binary_op<int32_t, Op>(a, b, out, bopt);
-        break;
-      case int64:
-        binary_op<int64_t, Op>(a, b, out, bopt);
-        break;
-      default:
-        throw std::runtime_error("[binary_int] Type not supported");
-        break;
-    }
-  });
+  encoder.dispatch(BinaryIntTask<Op>{array::unsafe_weak_copy(a),
+      array::unsafe_weak_copy(b), array::unsafe_weak_copy(out), bopt});
 }
 
 } // namespace mlx::core
diff --git a/mlx/backend/cpu/binary.cpp b/mlx/backend/cpu/binary.cpp
--- a/mlx/backend/cpu/binary.cpp
+++ b/mlx/backend/cpu/binary.cpp
@@ -341,6 +341,13 @@
     size_t elements, EvalDispatchStorage& out) noexcept {
   if (!binary_dtype(dtype)) return false;
   switch (selected) {
+#define INTEGER(Kind, Op) case BinaryEvalKind::Kind: return (dtype == uint32 || dtype == int8) && binary_task_storage<BinaryIntTask<::mlx::core::detail::Op>>(rank,elements,out);
+    INTEGER(bitwise_and, BitwiseAnd)
+    INTEGER(bitwise_or, BitwiseOr)
+    INTEGER(bitwise_xor, BitwiseXor)
+    INTEGER(left_shift, LeftShift)
+    INTEGER(right_shift, RightShift)
+#undef INTEGER
     case BinaryEvalKind::log_add_exp:
       // This is the ordinary floating worker's exact named task, including
       // its three weak operands and existing fixed collapse/iterator source.
diff --git a/mlx/backend/cpu/binary_storage.h b/mlx/backend/cpu/binary_storage.h
--- a/mlx/backend/cpu/binary_storage.h
+++ b/mlx/backend/cpu/binary_storage.h
@@ -4,7 +4,7 @@
 namespace mlx::core::cpu {
 enum class BinaryEvalKind {
   add, subtract, multiply, divide, maximum, minimum, power,
-  logical_and, logical_or, equal, not_equal, greater, greater_equal, less, less_equal, log_add_exp, remainder
+  logical_and, logical_or, equal, not_equal, greater, greater_equal, less, less_equal, log_add_exp, remainder, bitwise_and, bitwise_or, bitwise_xor, left_shift, right_shift
 };
 struct BinaryEvalStorage : SingleOutputEvalStorage {
   BinaryEvalKind kind{BinaryEvalKind::add};
diff --git a/mlx/backend/cpu/binary_storage.cpp b/mlx/backend/cpu/binary_storage.cpp
--- a/mlx/backend/cpu/binary_storage.cpp
+++ b/mlx/backend/cpu/binary_storage.cpp
@@ -26,6 +26,15 @@
 #undef SIMPLE
   if (typeid(primitive) == typeid(Equal) && !static_cast<const Equal&>(primitive).state()) {
     out = BinaryEvalKind::equal; return true;
+  }
+  if (typeid(primitive) == typeid(BitwiseBinary)) {
+    switch (static_cast<const BitwiseBinary&>(primitive).state()) {
+      case BitwiseBinary::And: out=BinaryEvalKind::bitwise_and; return true;
+      case BitwiseBinary::Or: out=BinaryEvalKind::bitwise_or; return true;
+      case BitwiseBinary::Xor: out=BinaryEvalKind::bitwise_xor; return true;
+      case BitwiseBinary::LeftShift: out=BinaryEvalKind::left_shift; return true;
+      case BitwiseBinary::RightShift: out=BinaryEvalKind::right_shift; return true;
+    }
   }
   return false;
 }
diff --git a/mlx/backend/cpu/gather_storage.cpp b/mlx/backend/cpu/gather_storage.cpp
--- a/mlx/backend/cpu/gather_storage.cpp
+++ b/mlx/backend/cpu/gather_storage.cpp
@@ -44,7 +44,8 @@
       (source==float16&&source.size()==2)||
       (source==int32&&source.size()==4)||(source==uint32&&source.size()==4)||
       (source==bool_&&source.size()==1))||
-      !((index==uint32&&index.size()==4)||(index==int32&&index.size()==4))||
+      !((index==uint32&&index.size()==4)||(index==int32&&index.size()==4)||
+      (index==int8&&index.size()==1))||
       !source_rank||source_rank>4||index_rank>5-source_rank||
       source_elements>size_t(INT_MAX)||(!source_elements&&index_elements)||
       index_elements>size_t(INT_MAX)||(!index_rank&&index_elements!=1)||
diff --git a/mlx/backend/cpu/concatenate_storage.cpp b/mlx/backend/cpu/concatenate_storage.cpp
--- a/mlx/backend/cpu/concatenate_storage.cpp
+++ b/mlx/backend/cpu/concatenate_storage.cpp
@@ -20,7 +20,7 @@
 }
 bool concatenate_many_eval_layout(Dtype dtype,size_t rank,size_t inputs,size_t elements,bool tracer,CopyEvalStorage& out) noexcept {
   if(!((dtype==float32&&dtype.size()==4)||(dtype==float16&&dtype.size()==2)||
-      (dtype==bfloat16&&dtype.size()==2)||(dtype==uint8&&dtype.size()==1)||(dtype==int32&&dtype.size()==4))||rank<1||rank>4||
+      (dtype==bfloat16&&dtype.size()==2)||(dtype==uint8&&dtype.size()==1)||(dtype==int32&&dtype.size()==4)||(dtype==uint32&&dtype.size()==4))||rank<1||rank>4||
       inputs<2||inputs>size_t(INT_MAX)||elements>size_t(INT_MAX)||inputs>(SIZE_MAX-1)/2)return false;
   const size_t weak_copies=inputs*2;
   CopyEvalStorage value;
diff --git a/mlx/backend/cpu/copy_storage.cpp b/mlx/backend/cpu/copy_storage.cpp
--- a/mlx/backend/cpu/copy_storage.cpp
+++ b/mlx/backend/cpu/copy_storage.cpp
@@ -304,7 +304,7 @@
 
 namespace {
 bool byte_view_dtype(Dtype dtype) noexcept {
-  return (dtype==uint8&&dtype.size()==1)||
+  return ((dtype==uint8||dtype==int8)&&dtype.size()==1)||
       ((dtype==int32||dtype==uint32||dtype==float32)&&dtype.size()==4)||
       ((dtype==float16||dtype==bfloat16)&&dtype.size()==2);
 }
diff --git a/mlx/backend/cpu/alias_storage.cpp b/mlx/backend/cpu/alias_storage.cpp
--- a/mlx/backend/cpu/alias_storage.cpp
+++ b/mlx/backend/cpu/alias_storage.cpp
@@ -19,6 +19,7 @@
 bool canonical_dtype(const array& value) noexcept {
   return (value.dtype()==bool_&&value.itemsize()==sizeof(bool))||
       (value.dtype()==uint8&&value.itemsize()==sizeof(uint8_t))||
+      (value.dtype()==int8&&value.itemsize()==sizeof(int8_t))||
       (value.dtype()==int32&&value.itemsize()==4)||
       (value.dtype()==float32&&value.itemsize()==4)||
       (value.dtype()==float16&&value.itemsize()==2)||
