# Audited LFM2 protocol output

`candidate-status-b3afba27.txt` is the function-call span from the official
`LiquidAI/LFM2-350M` model card at pinned revision
`b3afba27815ee83a64b76162cef4d8a4780d6ca7`. It demonstrates that the released
assistant syntax is a Python expression list, rather than a JSON object or
JSON list.
