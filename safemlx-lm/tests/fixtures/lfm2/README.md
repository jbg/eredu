# LFM2 protocol fixtures

These fixed model-output spans exercise LFM2's Python-expression tool-call
syntax. They verify that the released call surface is parsed as a Python call
list rather than JSON and remains separate from the declarative JSON dialects.
