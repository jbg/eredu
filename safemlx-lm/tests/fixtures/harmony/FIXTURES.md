# Harmony protocol fixtures

These raw protocol spans cover Harmony reasoning, commentary, recipient
metadata, function calls, prior call results, and action terminators. They test
the stateful channel-aware parser and its sampling-stop behavior.
