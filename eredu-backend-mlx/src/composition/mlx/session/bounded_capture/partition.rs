//! Shared receipt source and native transport contract for admitted model frames.
//!
//! The frame worker remains the same one used by text admission. Each caller
//! supplies genuine native transport custody and already protected row owners.
pub(in crate::composition::mlx) use crate::composition::mlx::session::model_session::text_quote::capture::partition::{
    ModelEvidenceHostSource, ModelEvidenceHosts, NativePartitionCaptureTransport, PartitionCaptureRunSource, PartitionCaptureFrame, PartitionCaptureInvocation, PartitionCaptureInvocationSource, PartitionCaptureRowMode,
};

pub(in crate::composition::mlx) mod native;
